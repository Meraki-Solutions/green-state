{
  "name": "@symbiotic/state",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm run lint && mkdir -p dist && tsc -p tsconfig.json",
    "build:watch": "mkdir -p dist && tsc -p tsconfig.json --watch",
    "lint": "tslint -p tsconfig.json"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/react": "^16.8.16",
    "tslint": "^5.16.0",
    "typescript": "^3.4.5"
  },
  "peerDependencies": {
    "react": "^16.8.6"
  },
  "dependencies": {
    "aurelia-dependency-injection": "^1.4.2",
    "reflect-metadata": "^0.1.13"
  }
}
